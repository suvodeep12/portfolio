# Suvodeep Ghosh portfolio

Static Astro portfolio for full-stack products, useful tools, and interactive software.

## Commands

```sh
npm install
npm run dev
npm run build
npm run preview
```

Project pages live in `src/content/projects/` as Markdown files. The site currently uses a hand-written collection; GitHub sync is intentionally not part of version one.
